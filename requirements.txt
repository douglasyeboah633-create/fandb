# LandPro Records Management System - Python dependencies
Django==6.1.1
dj-database-url==3.1.2
python-dotenv==1.2.3
whitenoise==6.12.0
reportlab==5.0.1
openpyxl==3.1.5

# Production WSGI server (Render, Railway, Fly.io, VPS). Linux only -
# the Windows local launcher uses Django's own server instead.
gunicorn==23.0.0

# PostgreSQL driver (required by Django when DATABASE_URL is PostgreSQL).
psycopg[binary]==3.2.3
